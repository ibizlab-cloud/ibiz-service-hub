package net.ibizsys.central.security;

/**
 * 系统角色默认用户默认
 * @author lionlau
 *
 */
public class SysRoleDefaultUsers {

	 /**
     *  无，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String NONE = "NONE";
    /**
     *  授权访问用户，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String ACCESSUSER = "ACCESSUSER";
    /**
     *  授权访问用户（管理员），注意：值为对象值，不能直接用于 == 比较
     */
    public final static String ACCESSADMIN = "ACCESSADMIN";
    /**
     *  用户默认，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String USER = "USER";
    /**
     *  管理员默认，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String ADMIN = "ADMIN";
    
}
