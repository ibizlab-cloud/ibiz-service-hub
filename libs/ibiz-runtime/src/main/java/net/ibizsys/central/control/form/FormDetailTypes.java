package net.ibizsys.central.control.form;

/**
 * 表单成员类型集合
 * @author lionlau
 *
 */
public class FormDetailTypes {

	/**
     *  表单分页，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String FORMPAGE = "FORMPAGE";
    /**
     *  分页部件，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String TABPANEL = "TABPANEL";
    /**
     *  分页面板，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String TABPAGE = "TABPAGE";
    /**
     *  表单项，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String FORMITEM = "FORMITEM";
    /**
     *  用户控件，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String USERCONTROL = "USERCONTROL";
    /**
     *  表单部件，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String FORMPART = "FORMPART";
    /**
     *  分组面板，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String GROUPPANEL = "GROUPPANEL";
    /**
     *  数据关系界面，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String DRUIPART = "DRUIPART";
    /**
     *  直接内容，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String RAWITEM = "RAWITEM";
    /**
     *  表单按钮，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String BUTTON = "BUTTON";
    /**
     *  直接页面嵌入，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String IFRAME = "IFRAME";
    /**
     *  复合表单项，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String FORMITEMEX = "FORMITEMEX";
    /**
     *  多数据部件，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String MDCTRL = "MDCTRL";
    
}
