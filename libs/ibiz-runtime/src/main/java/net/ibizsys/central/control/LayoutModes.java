package net.ibizsys.central.control;

public class LayoutModes {

	/**
     *  栅格布局（12列均分），注意：值为对象值，不能直接用于 == 比较
     */
    public final static String TABLE_12COL = "TABLE_12COL";
    /**
     *  栅格布局（24列均分），注意：值为对象值，不能直接用于 == 比较
     */
    public final static String TABLE_24COL = "TABLE_24COL";
    /**
     *  Flex布局，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String FLEX = "FLEX";
    /**
     *  边缘布局，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String BORDER = "BORDER";
    /**
     *  表格，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String TABLE = "TABLE";
    /**
     *  自动表格，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String AUTOTABLE = "AUTOTABLE";
}
