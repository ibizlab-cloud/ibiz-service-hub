package net.ibizsys.central.testing;




/**
 * 静态代码表[测试用例目标类型]模型基类
 */
public class TestCaseTargetTypes {
    /**
     *  实体属性值规则，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String DEFVR = "DEFVR";
    /**
     *  实体行为，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String DEACTION = "DEACTION";
    /**
     *  实体接口方法，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String DESADETAIL = "DESADETAIL";
    /**
     *  应用视图，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String APPVIEW = "APPVIEW";
    /**
     *  自定义，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String CUSTOM = "CUSTOM";

   

}