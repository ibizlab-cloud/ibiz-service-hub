package net.ibizsys.central.dataentity.logic;

/**
 * 实体逻辑连接单项条件参数类型
 * @author lionlau
 *
 */
public class DELogicLinkSingleCondParamTypes {

	 /**
     *  目标逻辑参数属性，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String ENTITYFIELD = "ENTITYFIELD";
    /**
     *  当前时间，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String CURTIME = "CURTIME";
    
    /**
     *  源逻辑参数属性，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String SRCENTITYFIELD = "SRCENTITYFIELD";
    
    
    /**
     *  源逻辑参数，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String SRCDLPARAM = "SRCDLPARAM";
    
    
    /**
     *  最后调用返回值，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String LASTRETURN = "LASTRETURN";
    
}
