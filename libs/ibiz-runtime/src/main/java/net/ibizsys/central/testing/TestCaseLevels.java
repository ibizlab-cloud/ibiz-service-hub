package net.ibizsys.central.testing;


/**
 * 静态代码表[测试用例级别]模型基类
 */
public class TestCaseLevels{
    /**
     *  P1，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String P1 = "P1";
    /**
     *  P2，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String P2 = "P2";
    /**
     *  P3，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String P3 = "P3";
    /**
     *  P4，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String P4 = "P4";
    /**
     *  P5，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String P5 = "P5";


}