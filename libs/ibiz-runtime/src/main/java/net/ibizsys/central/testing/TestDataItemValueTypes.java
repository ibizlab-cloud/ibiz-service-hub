package net.ibizsys.central.testing;




/**
 * 静态代码表[测试数据项值类型]模型基类
 */
public class TestDataItemValueTypes {
    /**
     *  直接值，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String VALUE = "VALUE";
    /**
     *  值范围，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String VALUERANGE = "VALUERANGE";
    /**
     *  空值，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String NULLVALUE = "NULLVALUE";
    /**
     *  外键随机值，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String PICKUPVALUE = "PICKUPVALUE";
    /**
     *  代码表随机值，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String CODELISTVALUE = "CODELISTVALUE";

    
}