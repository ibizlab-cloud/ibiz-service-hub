package net.ibizsys.central.control.menu;

public class AppMenuItemTypes {

    /**
     *  分隔项，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String SEPERATOR = "SEPERATOR";
    /**
     *  用户自定义项，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String USERITEM = "USERITEM";
    /**
     *  菜单引用，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String APPMENUREF = "APPMENUREF";
    /**
     *  菜单项，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String MENUITEM = "MENUITEM";
    /**
     *  直接内容项，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String RAWITEM = "RAWITEM";
    
}
