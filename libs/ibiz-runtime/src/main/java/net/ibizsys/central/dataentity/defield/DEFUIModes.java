package net.ibizsys.central.dataentity.defield;

/**
 * 属性界面模式
 * @author lionlau
 *
 */
public class DEFUIModes {

	/**
     *  默认模式，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String DEFAULT = "DEFAULT";
    /**
     *  用户自定义，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String CUSTOM = "CUSTOM";
    /**
     *  移动端默认模式，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String MOBILEDEFAULT = "MOBILEDEFAULT";
    /**
     *  应用默认模式，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String APPDEFAULT = "APPDEFAULT";
    /**
     *  模式1，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String MODE1 = "MODE1";
    /**
     *  模式2，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String MODE2 = "MODE2";
    /**
     *  模式3，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String MODE3 = "MODE3";
    /**
     *  模式4，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String MODE4 = "MODE4";
    /**
     *  模式5，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String MODE5 = "MODE5";
    /**
     *  模式6，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String MODE6 = "MODE6";
    /**
     *  模式7，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String MODE7 = "MODE7";
    /**
     *  模式8，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String MODE8 = "MODE8";
    /**
     *  模式9，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String MODE9 = "MODE9";
    
}
