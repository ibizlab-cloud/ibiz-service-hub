package net.ibizsys.central.dataentity.demap;

public class DEMapFieldMapTypes {

    /**
     *  属性等价，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String FIELD = "FIELD";
    /**
     *  直接值到目标属性，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String VALUE = "VALUE";
    /**
     *  计算值到目标属性，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String EXPRESSION = "EXPRESSION";
    /**
     *  直接值到源属性，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String VALUE_SRC = "VALUE_SRC";
    /**
     *  计算值到源属性，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String EXPRESSION_SRC = "EXPRESSION_SRC";
    
}
