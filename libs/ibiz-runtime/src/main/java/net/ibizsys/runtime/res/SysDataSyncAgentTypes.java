package net.ibizsys.runtime.res;

/**
 * 系统数据同步代理类型
 * @author lionlau
 *
 */
public class SysDataSyncAgentTypes {

	 /**
     *  Kafka，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String KAFKA = "KAFKA";
    
    /**
     *  RabbitMQ，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String RABBITMQ = "RABBITMQ";
    
	 /**
     *  ActiveMQ，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String ACTIVEMQ = "ACTIVEMQ";
    
    /**
     *  RocketMQ，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String ROCKETMQ = "ROCKETMQ";
    
    /**
     *  MQTT，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String MQTT = "MQTT";
    
    /**
     *  STOMP，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String STOMP = "STOMP";
    
    /**
     *  Ws，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String WS = "WS";
    
    
    /**
     *  内部通讯，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String INTERNAL = "INTERNAL";
    
    
    
    
    /**
     *  用户自定义，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String USER = "USER";
    /**
     *  用户自定义2，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String USER2 = "USER2";
}
