package net.ibizsys.runtime.dataentity.action;

public class DEActionParamModes {

    /**
     *  全部参数，注意：值为对象值，不能直接用于 == 比较
     */
    public final static int ALL = 1;
    /**
     *  指定参数（主键默认传入），注意：值为对象值，不能直接用于 == 比较
     */
    public final static int SOME = 2;
    
}
