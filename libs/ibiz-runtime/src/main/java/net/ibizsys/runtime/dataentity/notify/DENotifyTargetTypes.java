package net.ibizsys.runtime.dataentity.notify;

/**
 * 实体通知目标类型
 * @author lionlau
 *
 */
public class DENotifyTargetTypes {

    /**
     *  当前实体属性，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String DEFIELD = "DEFIELD";
    /**
     *  系统消息目标，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String SYSMSGTARGET = "SYSMSGTARGET";
    /**
     *  用户自定义，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String USER = "USER";
    /**
     *  用户自定义2，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String USER2 = "USER2";
    
}
