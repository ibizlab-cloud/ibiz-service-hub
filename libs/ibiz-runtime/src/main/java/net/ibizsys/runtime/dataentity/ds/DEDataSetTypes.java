package net.ibizsys.runtime.dataentity.ds;

public class DEDataSetTypes {

	/**
     *  数据查询，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String DATAQUERY = "DATAQUERY";
    /**
     *  索引实体，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String INDEXDE = "INDEXDE";
    /**
     *  多表单，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String MULTIFORM = "MULTIFORM";
    /**
     *  代码表，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String CODELIST = "CODELIST";
    
    /**
     *  实体逻辑，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String DELOGIC = "DELOGIC";
    
    /**
     *  脚本代码，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String SCRIPT = "SCRIPT";
    /**
     *  远程接口数据集，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String REMOTE = "REMOTE";
}
