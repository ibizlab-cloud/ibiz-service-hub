package net.ibizsys.runtime.dataentity.defield;


/**
 * 实体属性组类型
 * @author lionlau
 *
 */
public class DEFGroupTypes {

	  /**
     *  指定属性，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String FIELDS = "FIELDS";
    /**
     *  表单项，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String FORMITEMS = "FORMITEMS";
    /**
     *  基础属性，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String BASEFIELDS = "BASEFIELDS";
    /**
     *  审计属性，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String AUDITFIELDS = "AUDITFIELDS";
    
}
