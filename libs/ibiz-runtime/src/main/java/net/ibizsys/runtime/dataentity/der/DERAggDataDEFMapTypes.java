package net.ibizsys.runtime.dataentity.der;

public class DERAggDataDEFMapTypes {

	 /**
     *  合计，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String SUM = "SUM";
    /**
     *  平均，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String AVG = "AVG";
    /**
     *  最大值，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String MAX = "MAX";
    /**
     *  最小值，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String MIN = "MIN";
    /**
     *  计数，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String COUNT = "COUNT";

    /**
     *  分组，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String GROUPBY = "GROUPBY";
    /**
     *  按周天分组，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String GROUPBY_DAYOFWEEK = "GROUPBY_DAYOFWEEK";
    /**
     *  按月天分组，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String GROUPBY_DAYOFMONTH = "GROUPBY_DAYOFMONTH";
    /**
     *  按年天分组，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String GROUPBY_DAYOFYEAR = "GROUPBY_DAYOFYEAR";
    /**
     *  按周分组，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String GROUPBY_WEEK = "GROUPBY_WEEK";
    /**
     *  按月份分组，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String GROUPBY_MONTH = "GROUPBY_MONTH";
    /**
     *  按季度分组，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String GROUPBY_QUARTER = "GROUPBY_QUARTER";
    /**
     *  按年份分组，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String GROUPBY_YEAR = "GROUPBY_YEAR";
    /**
     *  按小时分组，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String GROUPBY_HOUR = "GROUPBY_HOUR";
    /**
     *  按分钟分组，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String GROUPBY_MINUTE = "GROUPBY_MINUTE";
    /**
     *  用户自定义，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String USER = "USER";
    /**
     *  用户自定义2，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String USER2 = "USER2";
    /**
     *  用户自定义3，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String USER3 = "USER3";
    /**
     *  用户自定义4，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String USER4 = "USER4";
}
