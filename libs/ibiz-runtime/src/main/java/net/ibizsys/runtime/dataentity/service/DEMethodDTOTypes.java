package net.ibizsys.runtime.dataentity.service;

/**
 * 实体方法DTO类型
 * @author lionlau
 *
 */
public class DEMethodDTOTypes {

	 /**
     *  实体默认，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String DEFAULT = "DEFAULT";
    /**
     *  实体行为自定义参数，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String DEACTIONINPUT = "DEACTIONINPUT";
    /**
     *  实体过滤器，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String DEFILTER = "DEFILTER";
    
    /**
     *  实体数据集自定义参数，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String DEDATASETINPUT = "DEDATASETINPUT";
    
}
