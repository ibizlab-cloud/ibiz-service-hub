package net.ibizsys.runtime.backend;

/**
 * 系统后台任务类型
 * @author lionlau
 *
 */
public class SysBackendTaskTypes {

	/**
     *  预定义，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String PREDEFINED = "PREDEFINED";
    /**
     *  触发实体行为，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String DEACTION = "DEACTION";
    /**
     *  自定义，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String USER = "USER";
    
}
