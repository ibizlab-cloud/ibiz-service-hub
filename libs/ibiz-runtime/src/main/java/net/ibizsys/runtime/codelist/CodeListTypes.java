package net.ibizsys.runtime.codelist;

/**
 * 代码表类型
 * @author lionlau
 *
 */
public class CodeListTypes {

	 /**
     *  静态，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String STATIC = "STATIC";
    /**
     *  动态，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String DYNAMIC = "DYNAMIC";
    /**
     *  预定义，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String PREDEFINED = "PREDEFINED";
    
	 /**
     *  内联静态，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String INLINE = "INLINE";
}
