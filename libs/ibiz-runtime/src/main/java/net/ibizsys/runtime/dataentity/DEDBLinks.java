package net.ibizsys.runtime.dataentity;

/**
 * 实体数据库链接
 * @author lionlau
 *
 */
public class DEDBLinks {

	/**
     *  默认连接，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String DEFAULT = "DEFAULT";
    /**
     *  数据连接2，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String DB2 = "DB2";
    /**
     *  数据连接3，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String DB3 = "DB3";
    /**
     *  数据连接4，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String DB4 = "DB4";
    /**
     *  数据连接5，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String DB5 = "DB5";
    /**
     *  数据连接6，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String DB6 = "DB6";
    /**
     *  数据连接7，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String DB7 = "DB7";
    /**
     *  数据连接8，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String DB8 = "DB8";
    /**
     *  数据连接9，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String DB9 = "DB9";
    /**
     *  数据连接10，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String DB10 = "DB10";
    /**
     *  数据连接11，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String DB11 = "DB11";
    /**
     *  数据连接12，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String DB12 = "DB12";
    
}
