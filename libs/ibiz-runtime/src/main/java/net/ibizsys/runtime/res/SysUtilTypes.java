package net.ibizsys.runtime.res;

public class SysUtilTypes {

	/**
     *  数据审计，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String DATAAUDIT = "DATAAUDIT";
    /**
     *  附件存储，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String FILE = "FILE";
    /**
     *  应用自定义，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String APPCUSTOMIZE = "APPCUSTOMIZE";
    /**
     *  SaaS应用管理，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String SAASADMIN = "SAASADMIN";
    /**
     *  SaaS用户授权（内置），注意：值为对象值，不能直接用于 == 比较
     */
    public final static String SAASUSERAUTH = "SAASUSERAUTH";
    /**
     *  SaaS用户授权服务（对外），注意：值为对象值，不能直接用于 == 比较
     */
    public final static String SAASUSERAUTHSERVICE = "SAASUSERAUTHSERVICE";
    /**
     *  SaaS组织功能（内置），注意：值为对象值，不能直接用于 == 比较
     */
    public final static String SAASORG = "SAASORG";
    /**
     *  SaaS组织服务（对外），注意：值为对象值，不能直接用于 == 比较
     */
    public final static String SAASORGSERVICE = "SAASORGSERVICE";
    /**
     *  SaaS流程引擎功能（内置），注意：值为对象值，不能直接用于 == 比较
     */
    public final static String SAASWF = "SAASWF";
    /**
     *  SaaS流程引擎服务（对外），注意：值为对象值，不能直接用于 == 比较
     */
    public final static String SAASWFSERVICE = "SAASWFSERVICE";
    /**
     *  SaaS核心服务（对外），注意：值为对象值，不能直接用于 == 比较
     */
    public final static String SAASCORESERVICE = "SAASCORESERVICE";
    
    
    /**
     *  日志侦听器，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String LOGLISTENER = "LOGLISTENER";
    
    
    /**
     *  用户自定义，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String USER = "USER";
    
}
