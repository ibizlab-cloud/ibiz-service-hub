package net.ibizsys.runtime.dataentity.util;

/**
 * 实体功能类型
 * @author lionlau
 *
 */
public class DEUtilTypes {

	 /**
     *  数据审计，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String DATAAUDIT = "DATAAUDIT";
    /**
     *  动态存储，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String DYNASTORAGE = "DYNASTORAGE";
    
    /**
     *  实体扩展，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String EXTENSION = "EXTENSION";
    
    /**
     *  通知设置，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String NOTIFYSETTING = "NOTIFYSETTING";
    
    /**
     *  版本控制，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String VERSIONCONTROL = "VERSIONCONTROL";
    
    /**
     *  版本存储，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String VERSIONSTORAGE = "VERSIONSTORAGE";
    
    /**
     *  缓存，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String CACHE = "CACHE";
    
    /**
     *  用户自定义，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String USER = "USER";
    
}
