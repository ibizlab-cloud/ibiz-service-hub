package net.ibizsys.runtime.dataentity.report;

/**
 * 实体报表类型
 * @author lionlau
 *
 */
public class DEReportTypes {

	 /**
     *  POI模板引擎，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String POI_TL = "POI_TL";
    /**
     *  AntVG6，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String ANTVG6 = "ANTVG6";
    /**
     *  LuckySheet，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String LUCKYSHEET = "LUCKYSHEET";
    /**
     *  AvueData，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String AVUEDATA = "AVUEDATA";
    /**
     *  AvueData（Cloud），注意：值为对象值，不能直接用于 == 比较
     */
    public final static String AVUEDATA_CLOUD = "AVUEDATA_CLOUD";
    /**
     *  Grafana，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String GRAFANA = "GRAFANA";
    /**
     *  DataEase，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String DATAEASE = "DATAEASE";
    /**
     *  JasperReport，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String JR = "JR";
    /**
     *  HTML，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String HTML = "HTML";
    /**
     *  Markdown，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String MARKDOWN = "MARKDOWN";
    /**
     *  系统智能报表立方体，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String SYSBICUBE = "SYSBICUBE";
    /**
     *  系统智能报表立方体（实体相关），注意：值为对象值，不能直接用于 == 比较
     */
    public final static String DESYSBICUBES = "DESYSBICUBES";
    /**
     *  系统智能报表立方体（全部），注意：值为对象值，不能直接用于 == 比较
     */
    public final static String ALLSYSBICUBES = "ALLSYSBICUBES";
    /**
     *  系统智能报表，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String SYSBIREPORT = "SYSBIREPORT";
    /**
     *  系统智能报表（实体相关），注意：值为对象值，不能直接用于 == 比较
     */
    public final static String DESYSBIREPORTS = "DESYSBIREPORTS";
    /**
     *  系统智能报表（立方体相关），注意：值为对象值，不能直接用于 == 比较
     */
    public final static String SYSBICUBEREPORTS = "SYSBICUBEREPORTS";
    /**
     *  系统智能报表（全部），注意：值为对象值，不能直接用于 == 比较
     */
    public final static String ALLSYSBIREPORTS = "ALLSYSBIREPORTS";
    /**
     *  用户自定义，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String USER = "USER";
    /**
     *  用户自定义2，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String USER2 = "USER2";
    /**
     *  用户自定义3，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String USER3 = "USER3";
    /**
     *  用户自定义4，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String USER4 = "USER4";

    
}
