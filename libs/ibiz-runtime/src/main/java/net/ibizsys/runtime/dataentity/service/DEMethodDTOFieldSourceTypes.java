package net.ibizsys.runtime.dataentity.service;

public class DEMethodDTOFieldSourceTypes {

	/**
     *  实体属性，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String DEFIELD = "DEFIELD";
    /**
     *  实体属性组成员，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String DEFGROUPDETAIL = "DEFGROUPDETAIL";
    /**
     *  实体关系，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String DER = "DER";
    /**
     *  动态模型属性，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String DYNAMODELATTR = "DYNAMODELATTR";
    /**
     *  实体行为参数，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String DEACTIONPARAM = "DEACTIONPARAM";
    /**
     *  属性搜索模式，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String DEFSEARCHMODE = "DEFSEARCHMODE";
    
}
