package net.ibizsys.runtime.dataentity.defield;

/**
 * 属性预定义类型
 * 
 * @author lionlau
 *
 */
public class DEFPredefinedTypes {

	/**
	 * （非预置属性），注意：值为对象值，不能直接用于 == 比较
	 */
	public final static String NONE = "NONE";
	/**
	 * 逻辑有效标识，注意：值为对象值，不能直接用于 == 比较
	 */
	public final static String LOGICVALID = "LOGICVALID";
	/**
	 * 排序值，注意：值为对象值，不能直接用于 == 比较
	 */
	public final static String ORDERVALUE = "ORDERVALUE";
	/**
	 * 数据版本，注意：值为对象值，不能直接用于 == 比较
	 */
	public final static String VERSION = "VERSION";
	/**
	 * 建立人标识，注意：值为对象值，不能直接用于 == 比较
	 */
	public final static String CREATEMAN = "CREATEMAN";
	/**
	 * 建立人名称，注意：值为对象值，不能直接用于 == 比较
	 */
	public final static String CREATEMANNAME = "CREATEMANNAME";
	/**
	 * 建立时间，注意：值为对象值，不能直接用于 == 比较
	 */
	public final static String CREATEDATE = "CREATEDATE";
	/**
	 * 更新人标识，注意：值为对象值，不能直接用于 == 比较
	 */
	public final static String UPDATEMAN = "UPDATEMAN";
	/**
	 * 更新人名称，注意：值为对象值，不能直接用于 == 比较
	 */
	public final static String UPDATEMANNAME = "UPDATEMANNAME";
	/**
	 * 更新时间，注意：值为对象值，不能直接用于 == 比较
	 */
	public final static String UPDATEDATE = "UPDATEDATE";
	/**
	 * 组织机构标识，注意：值为对象值，不能直接用于 == 比较
	 */
	public final static String ORGID = "ORGID";
	/**
	 * 组织机构名称，注意：值为对象值，不能直接用于 == 比较
	 */
	public final static String ORGNAME = "ORGNAME";
	/**
	 * 部门标识，注意：值为对象值，不能直接用于 == 比较
	 */
	public final static String ORGSECTORID = "ORGSECTORID";
	/**
	 * 部门名称，注意：值为对象值，不能直接用于 == 比较
	 */
	public final static String ORGSECTORNAME = "ORGSECTORNAME";
	
	 /**
     *  动态父类型，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String PARENTTYPE = "PARENTTYPE";
    /**
     *  动态父标识，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String PARENTID = "PARENTID";
    /**
     *  动态父名称，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String PARENTNAME = "PARENTNAME";
    
    /**
     *  父标识路径，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String PARENTIDPATH = "PARENTIDPATH";
    /**
     *  父名称路径，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String PARENTNAMEPATH = "PARENTNAMEPATH";
    
    /**
     *  时间戳，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String TIMESTAMP = "TIMESTAMP";
    
    
    /**
     *  动态存储属性，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String DYNASTORAGE = "DYNASTORAGE";
    
    /**
     *  关闭标志属性，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String CLOSEFLAG = "CLOSEFLAG";
    
    /**
     *  锁定标志属性，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String LOCKFLAG = "LOCKFLAG";
    
}
