package net.ibizsys.central.cloud.core.util.domain;

/**
 * 开发系统类型
 * @author lionlau
 *
 */
public class DevSysTypes {

	  /**
     *  默认，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String DEVSYS = "DEVSYS";
    /**
     *  应用开发系统，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String DEVSYS_APP = "DEVSYS_APP";
    /**
     *  服务开发系统，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String DEVSYS_SVR = "DEVSYS_SVR";
    /**
     *  运行开发系统，注意：值为对象值，不能直接用于 == 比较
     */
    public final static String DEVSYS_DEPLOY = "DEVSYS_DEPLOY";
}
