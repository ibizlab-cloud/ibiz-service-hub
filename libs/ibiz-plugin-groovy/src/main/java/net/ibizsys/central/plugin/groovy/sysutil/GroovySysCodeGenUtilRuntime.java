package net.ibizsys.central.plugin.groovy.sysutil;

import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;

public class GroovySysCodeGenUtilRuntime extends GroovySysCodeGenUtilRuntimeBase {

	private static final Log log = LogFactory.getLog(GroovySysCodeGenUtilRuntime.class);
	
}
